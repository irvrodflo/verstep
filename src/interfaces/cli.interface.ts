export enum CliOperations {
  upgrade = 'up',
  sync = 'sync',
  reset = 'reset',
  rollback = 'rollback',
  init = 'init',
  version = 'version',
}

export enum Envronments {
  dev = '--dev',
  prod = '--prod',
}
